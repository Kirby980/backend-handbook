# Python 后端 · 综合测验

> 配套 [INDEX.md](./INDEX.md) 与 [ROADMAP.md](./ROADMAP.md)
>
> 共 75 题,覆盖 P01-P30 全部内容。难度按 ⭐ 个数分五档:⭐ 入门 → ⭐⭐⭐⭐⭐ 综合实战。
> 全部为开放题(面试问答风格),无标准答案——建议先合上书自己作答,再翻回对应章节对照;每篇文章末尾的「练习题参考答案」可作参考。

---

## 第一组:入门(12 题,⭐)

### 语言核心

1. Python 里"一切皆对象"是什么意思?对象有哪三个不变属性?
2. `is` 和 `==` 有什么区别?和 None 比较该用哪个?
3. 可变类型和不可变类型分别有哪些?这个区分有什么影响?
4. 列表(list)和元组(tuple)有什么区别?
5. `==` 比较的是什么?为什么 `0.1 + 0.2 != 0.3`?

### 函数与控制流

6. 什么是 LEGB?Python 有块级作用域吗?
7. `*args` 和 `**kwargs` 是什么?
8. 列表推导式和普通 for 循环相比有什么优势?
9. try/except/else/finally 各在什么时候执行?

### 面向对象

10. 类属性和实例属性有什么区别?
11. `self` 是什么?为什么方法第一个参数是 self?
12. 什么是鸭子类型?

---

## 第二组:基础(15 题,⭐⭐)

### 容器与字符串

13. dict 为什么平均 O(1)?为什么 3.7 后有序?
14. `str` 和 `bytes` 有什么区别?为什么不能直接混用?
15. 为什么循环里用 `+=` 拼接字符串慢?应该怎么做?
16. 什么时候用 collections.deque 而不是 list?

### 函数式与生成器

17. 可迭代对象(iterable)和迭代器(iterator)有什么区别?
18. `yield` 是什么?生成器为什么省内存?
19. 装饰器的本质是什么?`@deco` 等价于什么?
20. 闭包是什么?循环里创建闭包的"延迟绑定"陷阱是怎么回事?

### OOP 与异常

21. 默认可变参数(`def f(x, lst=[])`)为什么是坑?怎么修?
22. 重写 `__eq__` 为什么往往要同时定义 `__hash__`?
23. EAFP 和 LBYL 是什么?Python 推崇哪个?

### GIL 入门

24. GIL 是什么?它对多线程有什么影响?
25. CPU 密集和 IO 密集任务在 Python 里分别该用什么并发方式?
26. Python 的内存管理主要靠什么?
27. f-string、str.format、% 格式化有什么区别?

---

## 第三组:进阶(16 题,⭐⭐⭐)

### 对象模型

28. 描述 `obj.attr` 的属性查找顺序。
29. `__slots__` 是做什么的?有什么好处和代价?
30. 什么是 MRO?C3 线性化解决了什么问题?
31. `super()` 真的是"调用父类"吗?多继承里它调谁?

### 描述符与元类

32. 什么是描述符?数据描述符和非数据描述符的优先级如何?
33. 为什么说"方法自动绑定 self"是描述符机制的体现?
34. `@property` 的本质是什么?它解决了什么问题?
35. type 有哪两种用法?这说明了 Python 的什么设计?

### CPython 内幕

36. Python 是编译型还是解释型?描述源码到执行的过程。
37. 引用计数为什么处理不了循环引用?Python 怎么解决?
38. 什么是分代垃圾回收?为什么要分代?
39. CPython、PyPy 有什么区别?GIL 是 Python 语言的特性吗?

### 并发基础

40. "有 GIL 就不用加锁"对吗?举例说明。
41. threading 的 Lock 和 RLock、Semaphore 有什么区别?
42. 为什么 CPU 密集型任务要用多进程而不是多线程?
43. 为什么推荐用 queue.Queue 做线程间通信?

---

## 第四组:高级(18 题,⭐⭐⭐⭐)

### 类型与元编程

44. 类型注解在运行时会被强制检查吗?它有什么用?
45. `typing.Protocol` 和 ABC(抽象基类)有什么本质区别?
46. PEP 695(3.12)的泛型新语法是什么?
47. 元类能做什么?为什么说 `__init_subclass__` 能替代很多元类用例?

### 内存与性能

48. `sys.getsizeof` 返回的是真实总占用吗?
49. 处理上百万个数字时,为什么 NumPy/array 比 list 省内存?
50. 优化前为什么一定要先 profiling?常用工具有哪些?
51. Python 性能优化最该优先做什么?为什么算法比微优化重要?

### asyncio 深入

52. asyncio 的事件循环怎么调度协程?为什么单线程能高并发?
53. 协程和生成器有什么关系?
54. 为什么在协程里调用阻塞函数(time.sleep/requests)会有严重问题?
55. `await coro`、`create_task`、`gather`、`TaskGroup` 有什么区别?

### 多进程与选型

56. 进程间为什么不能直接共享变量?有哪些通信/共享方式?
57. 多进程的 pickle 序列化带来哪些限制和开销?
58. fork 和 spawn 启动方式有什么区别?为什么需要 `if __name__ == "__main__"`?
59. `ProcessPoolExecutor` 和 `ThreadPoolExecutor` 有什么关系和区别?
60. 多线程、多进程、asyncio 在内存共享和并行 CPU 上有什么根本不同?
61. 在异步程序里必须调用一个阻塞的同步函数,怎么办?

---

## 第五组:综合实战(14 题,⭐⭐⭐⭐⭐)

### free-threading 与并发选型

62. 什么是 free-threading(去 GIL)?它在 2026 处于什么状态?要注意什么?
63. free-threading 会如何改变 Python 的并发选型?
64. 给定一个高并发 IO 网关、和一个 CPU 密集的批处理任务,你分别会选什么并发模型?为什么?
65. 什么是子解释器和 per-interpreter GIL?它和 free-threading、多进程有什么关系?

### 工程化

66. pytest 的 fixture 是什么?yield、scope、conftest.py 各起什么作用?
67. mock 的 patch 最常见的坑是什么?为什么?
68. 为什么记录日志要用 `log.info("x=%s", x)` 而不是 f-string?在 except 里为什么用 `log.exception`?
69. requirements.txt 和锁文件(lock file)有什么区别?为什么需要锁文件?2026 为什么推荐 uv?

### Web 与数据

70. WSGI 和 ASGI 有什么区别?为什么会出现 ASGI?
71. FastAPI 为什么能自动做请求校验和生成文档?
72. 为什么必须用参数化查询?ORM 的 N+1 问题是什么、怎么解决?
73. 生产部署 FastAPI 通常怎么做(多进程 + 异步)?为什么这样?

### 综合贯通

74. 描述符、装饰器、类型注解三者如何配合支撑起现代 Python 框架(如 FastAPI/SQLAlchemy/Pydantic)?
75. 从 Python 3.8 升级到 3.13/3.14,你会关注哪些兼容性问题和新特性收益?(可结合 GIL/free-threading、类型、性能)
